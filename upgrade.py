#TODO install upgrades
# vc.set_policy()
# update_info = vc.get_component(version)
# precheck_info = vc.run_precheck(version)
# print(vc.validate(version=version))
# vc.stage_update(version=version)
# vc.run_install(version=version)